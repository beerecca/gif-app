<%= form_for(@gif) do |f| %>
  <% if @gif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gif.errors.count, "error") %> prohibited this gif from being saved:</h2>

      <ul>
      <% @gif.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <label>Gif Url</label>
    <%= f.text_area :img, class:'form-control' %>
  </div>
  <div class="field form-group">
    <label>Emotion</label>
    <%= f.text_area :emotion_list, class:'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit class:'btn btn-secondary'%>
  </div>
<% end %>
